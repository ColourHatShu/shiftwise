# 🛡️ Autonomous Knight — Backlog (priority-ordered)

> Top of the list = highest priority. The Knight ticks `[x]` when shipped,
> `[blocked] — reason` when it needs the human. **Verify each item isn't already
> implemented before building it** — the codebase moves between firings.
>
> Seeded from the 4-specialist Super-Ready Council audit (2026-05-27). Wave A
> (security/trust BLOCKERs) and several Wave B items already shipped in commit
> `ef91788`. This backlog is the remaining Wave B polish + Wave C features +
> quality work. The Knight may re-prioritise and add items as product owner.

## 🧭 NEXT MILESTONE — awaiting founder direction (2026-07-01)
The safely-buildable, non-gated backlog is **exhausted**. This session shipped a full quality foundation (CI + ~285 tests, structured logging, 3 production-class bug fixes, env/pagination hardening) **and three complete feature threads**: worker **reliability** (scorecards + assignment badges + suggested + profile panel), shift **coverage** (gaps view + dashboard alert), and the core-promise **expiring/overdue documents worklist**. Remaining work needs a founder decision — pick one and the loop runs with it:
- **AI shift-matcher** (the capstone) — ✅ **rule-based MVP shipped end-to-end** (`GET /api/shifts/:shiftId/suggested-workers` + "⭐ Top picks" strip in the assignment modal) using the documented default ranking (compliant → confirmation-rate desc → compliance score → name). **Founder input still welcome to tune the weights** (and to add distance/skills/rotation) — say the weights and the Knight will adjust.
- **Custom Helmet CSP** — ship **report-only first** (non-breaking). Greenlight: **"do the CSP (report-only)"**.
- **Auto-poster** — needs a spec of what it posts where. Greenlight: **"build the auto-poster"** + a one-line spec.
- **£ earnings / pay-rate model** — needs the rate data/business rules (per-worker or per-role hourly). Greenlight with the model.
- **Model-backed features** (each needs a small Prisma model + your nod): in-app **notifications centre**, **no-show/late incident** workflow, **care-home contact rolodex**, **coordinator handoff notes**.
- **Test infra:** make the `worker-e2e` / `security-pipeline` suites runnable (needs a **test DB** decision — CI Postgres service vs a dedicated Supabase test project).

Until a direction is given, further autonomous firings are marginal polish only — consider pausing the 10-min cron.

## P1 — Robustness, trust & accessibility
- [x] **(discovered P0)** Fix production build-breaker: `worker/dashboard/shifts/page.tsx` read `localStorage` at render time → `next build` failed for all pages. Moved to a post-mount `useEffect`.
- [x] Add a responsive mobile sidebar drawer to `frontend/app/dashboard/layout.tsx` (app is currently desktop-only at a fixed 220px sidebar)
- [x] Add `aria-label`s to all icon-only buttons across `frontend/app/` (19 buttons across 12 files; close/download/edit/delete/pagination controls)
- [x] Build a reusable `<Skeleton />` primitive and replace full-page spinners with skeleton loaders on the main list pages (workers, reports, shifts)
- [x] Add request-ID middleware on the backend and attach it as a Sentry correlation tag + return it in error responses — **already implemented** in `server.js` (commit `ef91788`): `req.requestId` + `X-Request-Id` header, Sentry tag, and `requestId` in error responses
- [x] Fix the N+1 query in bulk shift assignment (`backend/src/routes/shift-assignments.js`) — batched `validateComplianceForWorkers()` (constant 4 queries vs ~5×N); shared `computeCompliance()` keeps single/bulk paths identical

## P2 — Maintainability (DRY) & dead code
- [x] Extract the repeated Clerk `getToken + fetch + headers` boilerplate into a shared `useApi`/`apiFetch` helper and adopt it in the highest-traffic pages first — created `lib/use-api.ts`; adopted on dashboard, workers, documents pages
- [x] Migrate the remaining `getToken + fetch` sites to `useApi` — 9 files, 26 fetch sites migrated; only legit `getToken` left is for `lib/api/*` helpers (downloadDocument/getDocumentStatus/pollDocumentStatus) in `workers/[id]` + `documents`
- [x] Extract duplicated status-badge color logic into a single shared badge component/util — created `components/ui/status-badge.tsx` (`<StatusBadge>` + `getStatusStyle`); adopted on workers list, documents table, WorkerDetailModal
- [x] Dedupe shift-confirmation badges into `components/ui/confirmation-badge.tsx` (`<ConfirmationBadge>`), adopted in `AssignmentList` + `assigned-shifts`. **Decided against** folding worker-portal doc badges + worker-list RAG into one component: vocabularies collide ("PENDING" = amber "Pending Review" for docs vs gray "Pending" for confirmation), the worker portal uses a deliberately different palette, and RAG is a numeric score→bucket, not a status string.
- [x] Make `<Modal>` reusable for full header/body/footer modals (added `padded` prop) and migrated `DeleteConfirmationModal` onto it as the first faithful adoption (zero visual change + gained ESC/click-outside close)
- [x] **Finish modal consolidation** — founder chose design-system canonical (2026-06-30 "use design-system, go"). Restyled `<Modal>` to design-system (rounded-xl, border `#DDE3EE`, shadow-2xl, `bg-black/40 backdrop-blur` overlay) and migrated the 5 remaining bespoke modals onto it (`WorkerDetailModal`, `AuditPackModal`, `BulkUploadModal`, `ShiftModal`, `EditWorkerModal`). All modals now share `<Modal>`. ⚠️ Needs a visual pass.
- [x] Replace remaining native `window.confirm()` destructive actions with the styled `ConfirmDialog` — the last one (`AssignmentList` unassign-worker) now uses `<ConfirmDialog>`
- [x] Hide the dead `/dashboard/availability` route — removed the sidebar nav item (page was a non-persisting coordinator stub). Page kept as an unlinked placeholder for the P3 availability calendar.

## P3 — Feature expansion (Wave C)
- [x] Cmd+K command palette (first slice) — `components/ui/command-palette.tsx`, mounted in the dashboard layout; ⌘K/Ctrl+K, client-side fuzzy filter over navigation + quick actions, full keyboard nav
- [x] Cmd+K follow-ups: added a visible "Search… ⌘K" sidebar affordance (opens the palette via a custom event) + live **worker** search in the palette (debounced, via `useApi` → `/api/workers?search=`), unified keyboard nav across pages + workers
- [x] Extend Cmd+K live search to **shifts** (parallel debounced fetch via `/api/shifts?facilityName=`, grouped results). **Documents deliberately not searched** — no document search endpoint or detail route; doc hits would have no deep-link target distinct from worker search. Revisit only if a `/api/documents?search=` endpoint + a doc target are added.
- [x] Worker availability calendar — revived `/dashboard/availability` as a real, light-themed coordinator page (worker picker + month calendar) that **persists** AVAILABLE/UNAVAILABLE/ON_LEAVE via the existing `/api/workers/:id/availability` API; re-added the sidebar nav item
- [x] Re-theme the worker detail page (`dashboard/workers/[id]/page.tsx`) from dark (slate/white) to the light design system — converted ~100 className lines (cards→white/`#DDE3EE`, text→`#0A1628`/`#5B6E8C`, translucent-on-dark accents→light `*-50/*-700`), logic untouched. ⚠️ Needs a human visual pass (re-themed blind; build + grep verified, but not eyeballed).
- [x] Shift templates — **entity + backend API** slice: `ShiftTemplate` model (pushed to Supabase), `routes/shift-templates.js` CRUD (list/create/delete, agency-scoped) mounted at `/api/shift-templates`, +7 passing tests
- [x] Shift templates — **frontend slice:** new `/dashboard/shifts/templates` page (list/create/delete via `useApi` → `/api/shift-templates`) + per-template "pick a date → Create shift" action (POST `/api/shifts`); linked from the Shifts page header
- [blocked] Shift templates — **recurring auto-poster:** needs the human's go-ahead + a scheduling design decision. It's a **side-effecting cron** (auto-creates shifts on a cadence) — risky to build blind. **Knight's recommendation:** add recurrence fields to `ShiftTemplate` (cadence, daysOfWeek, postHorizonDays, active) + a daily cron in `cronService` that generates the next shifts and dedups against existing ones. Tell the Knight "build the auto-poster" to greenlight (then I'd do it as a council-designed multi-slice effort).
- [x] Worker shifts summary (the "earnings dashboard" item) — added a read-only summary row (Assigned / Upcoming / Completed / Total hours) to `/worker/dashboard/assigned-shifts`, computed from real assignment data. **No £ earnings** — there is no pay-rate data in the schema; showing money would be fabricated. See follow-up below.
- [ ] (If wanted) £ earnings — add a pay-rate model (per-worker or per-role/shift hourly rate) so the worker summary can show actual pay. Needs the human (rates are business data).

## P4 — Code quality & performance
- [x] Introduce a shared `frontend/types/api.ts` (Worker, DocumentType, ComplianceDocument, DocSlot, AnalysisResult, Shift, ShiftAssignment, ShiftTemplate, Paginated) and adopted it in `workers/[id]/page.tsx` — replaced the 9 worst `any`s (state vars, modal props, analysis result, slot, params). Build clean.
- [x] Continue `any` cleanup using `types/api.ts` — adopted shared types in `documents/page.tsx` (typed `workers`, `getComputedStatus`, `getComplianceScore`, the reduce/map callbacks; added `Worker.complianceDocuments`). Remaining app `any`s are mostly low-value `catch (err: any)`; shifts/compliance pages had ≤1 each.
- [x] `useMemo` heavy table rows — workers list rows are now `useMemo`'d over `[workers]` (with `getRAGStatus`/RAG colors hoisted to module scope for stable deps), so keystrokes in the local-state search box don't rebuild the table. Search was already debounced (300ms). Other tables (compliance/audit-log) can get the same treatment if they grow.
- [ ] Add a custom Helmet CSP to the backend — ⚠️ **risky blind:** a wrong enforcing CSP can white-screen the app by blocking Clerk/Sentry/Next scripts, and it only manifests at runtime in a browser (build/lint can't catch it). **Knight's recommendation:** ship it in **Report-Only** mode first (non-breaking, logs violations), enumerate the real origins (Clerk, Sentry, Supabase, fonts/images) from the reports, THEN flip to enforce. Needs a browser pass to confirm — greenlight with "do the CSP (report-only)".
- [x] Extend skeleton loaders to remaining full-page spinners — replaced the full-page spinners on `documents` + `audit-log` with layout-matching `<Skeleton>` loaders (role=status + sr-only). `audit-packs`, `compliance`, and the worker dashboard have **no full-page spinner** (inline/per-section loading), so nothing to change there.
- [x] Set `requestId` as a per-request Sentry scope tag (all events carry it) — **AND fixed a latent P0**: the Sentry setup used v7 APIs (`Sentry.Handlers.*`, `new Sentry.Integrations.*`) that are `undefined` in the installed `@sentry/node` v10, so enabling `SENTRY_DSN` would crash the backend on boot. Migrated to v8+/v10 (default integrations; dropped the dead requestHandler/errorHandler; manual `captureException` retained) + added a `Sentry.setTag('requestId', …)` middleware.
- [x] Fix the buggy `should reject non-OWNER/ADMIN users` test — the `requireAgency` mock now respects a pre-set `req.user` (defaults to OWNER), so the test can exercise a VIEWER → 403. `shift-assignments.test.js` is now 8/8; suite is 27 failed/131 passed (was 28/130).
- [ ] Fix the remaining broken backend suites — **3 of 5 done:** ✅ `worker-assignments` (missing `findMany` mock); ✅ `worker-auth` (rewrote → caught+fixed a **P0**: verify-code 500'd via invalid `iat` jwt option); ✅ `worker-dashboard` (rewrote → caught+fixed a **security bug**: `getWorkerDocuments`/`uploadWorkerDocument` read `req.worker.workerId` but the middleware sets `req.worker.id`, so the workerId filter was dropped → a worker saw **every worker's documents in their agency**; uploads also set workerId=undefined). All three rejoined `test:ci` (now 156 tests). **The quick-fix portion is DONE.** The 2 remaining suites are NOT quick fixes and are reclassified: `security-pipeline` is an **aspirational security SPEC suite** (mounts the real documents router, needs Clerk-auth mocking + `nock` + a live Ollama, and many tests deliberately assert *unbuilt* features — retry/backoff, wrong-doc detection, identity-mismatch, optimistic locking — plus env-coupled checks). Don't force-green it; harvest its assertions into real features (see IDEAS.md "Harvested from security-pipeline"). `worker-e2e` needs a live test DB. **Both stay excluded from `test:ci` until properly addressed.**

## P5 — Ideated 2026-06-30 (autonomously buildable, verifiable; not decision-gated)
- [x] **Manage Document Types — backend slice:** added `routes/document-types.js` (GET/POST/PATCH/DELETE, agency-scoped, dup-name 409, delete-blocked-if-in-use 409) mounted at `/api/document-types`, +8 tests. (Only a list endpoint existed before, at `/api/agencies/document-types`.)
- [x] **Manage Document Types — frontend slice:** `DocumentTypesManager` component on the Settings page — list/add/edit/delete document types (name, description, required?, hasExpiry, expiryWarningDays) via `/api/document-types` (`useApi`), ConfirmDialog delete with the in-use 409 surfaced as a toast. **Feature complete end-to-end.**
- [x] **Bulk worker CSV import — backend slice:** `routes/workers-bulk.js` (`POST /api/workers/bulk/upload` parse+validate+create with per-row errors & dup-email handling; `GET /api/workers/bulk/template`), mounted before the generic `/api/workers` router so `/bulk` isn't swallowed. +5 tests.
- [x] **Bulk worker CSV import — frontend slice:** `WorkerBulkUploadModal` (file upload + paste + download-template + per-row results/errors via `useApi`) wired to a "Bulk import" button on the Workers page header. **Feature complete end-to-end.**
- [x] **GitHub Actions CI** — `.github/workflows/ci.yml`: frontend job (`npm ci` → lint → build) + backend job (`npm ci` → `prisma generate` → `npm run test:ci`). Added a `test:ci` script that runs the 13 green suites (126 tests) and excludes the 5 currently-broken ones. Runs on push/PR to `main`.
- [x] **Modal focus management (a11y)** — shared `<Modal>` now focuses the first focusable element on open, traps Tab/Shift+Tab within the dialog, and restores focus to the trigger on close (card is `tabIndex=-1` as a fallback). Applies to every modal app-wide.
- [x] **Empty-state consistency — component + key pages:** created reusable `components/ui/empty-state.tsx` (`<EmptyState icon title message action>`, design-system styled). Adopted it on the **workers** table (no-match vs none-added, with CTA) and the **audit-log** (which was off-theme gray `text-gray-500`). Establishes the pattern.
- [x] **Empty-state consistency — remaining pages:** adopted `<EmptyState>` on **documents** (page-level, with CTA), **compliance** ("No workers found"), **shift templates** ("No templates yet"), and the **shifts detail** "no workers assigned" panel (was off-theme gray). Left **reports**' inline table-row messages as-is (they're contextual per-row notes, not blank states). Empty states are now consistent across all the main list pages.
- [x] **Harden startup env validation** — extracted a pure, unit-tested `lib/validate-env.js` (`getEnvErrors`): DATABASE_URL always required; JWT_SECRET (not the dev fallback) + CLERK_SECRET_KEY required in production; dev gets warnings. `server.js` now fails fast with a clear message + `process.exit(1)`. +5 tests.

## P6 — Core-logic coverage & hardening (2026-06-30)
- [x] **Unit-test `computeCompliance`** — 8 direct tests for the pure compliance/RAG function (no-required, all-approved, missing, not-yet-approved, expired, no-expiry, partial score + concatenated reasons, snapshot shape). Core product logic, previously only exercised indirectly via a route test. `test:ci` now 169 tests.
- [x] **Unit-test the compliance-dashboard list logic (frontend)** — 14 tests for `filterWorkers` (name/email/jobTitle, case-insensitive, empty/no-match), `sortWorkers` (score/name/updated, asc+desc, non-mutating), `paginateWorkers` (slice/totalPages/partial/empty), and `buildQueryString`/`parseQueryString` (defaults omitted + round-trip). These power the coordinator compliance list and were untested. Frontend `test:ci` 53 → **67**.
- [x] **Verified frontend Sentry is on the correct v10 API** — `app/providers/sentry-initializer.tsx` already uses functional `Sentry.replayIntegration()` / `Sentry.captureConsoleIntegration()` (v8+); NOT the removed v7 class API. No fix needed (unlike the backend, which the Knight fixed earlier). Recorded so future firings don't re-investigate.
- [x] **Harden background document analysis** — extracted `lib/analysis-failure.js` (`recordAnalysisFailure(doc, reason, error?)`: marks the doc FAILED, captures to Sentry, writes a `document.ai_analysis_failed` audit entry with the **reason only (no PII)**, never throws). Routed all 7 OCR/analysis failure paths in `routes/documents.js` through it (decrypt/PDF/OCR/unsupported-type/exception/outer-catch) and removed the now-dead `updateDocumentStatus`. +5 unit tests. (`analyzeDocument` already had an outer catch, so the gap was *silent* failures — no Sentry, no audit — not unhandled rejections; now every failure is visible + auditable.)

## P7 — Ideated 2026-06-30 (round 2; verified real gaps, buildable + test-verifiable)
- [x] **Identity-mismatch audit** — added `recordIdentityMismatch(doc, worker, detectedName)` to `lib/analysis-failure.js` → writes a `document.identity_mismatch_detected` audit entry (metadata: `{ expectedName, detectedName }` — names only, no document numbers). `analyzeDocument` calls it after a successful scan, **gated on `analysis.fullName && analysis.nameMatchesWorker === false`** so "no name extracted" never raises a false mismatch (the extractor defaults `nameMatchesWorker` to false). +3 unit tests (`test:ci` now 177).
- [x] **Frontend test runner** — installed **Vitest**, added `test`/`test:ci` scripts, a `vitest.config.ts` (scoped `include: ['lib/**/*.test.ts']`, node env), and a first unit test for `lib/compliance-dashboard.ts` (`getComplianceStatus` boundaries + `getStatusLabel`). Excluded `*.test.ts(x)` from `tsconfig` so `next build` doesn't typecheck them. Wired `npm run test:ci` into the CI frontend job. **Frontend now has a green test seam (4 tests).**
- [x] **Resurrect the pre-existing `frontend/__tests__/` suites (2 of 3)** — wired vitest with the `@/` alias, `globals:true`, `environment:'jsdom'`, and a `vitest.setup.ts` that shims `jest`→`vi`; installed `jsdom`. **`worker-compliance` (21) and `worker-offline` (16) now run green** (no bugs surfaced — the libs are correct). Frontend test count 4 → **41**.
- [x] **Resurrect `__tests__/audit-pack-components.test.tsx`** — installed `@testing-library/react` + `user-event` + `jest-dom` + `@vitejs/plugin-react` (JSX transform; tsconfig is jsx:preserve), added jest-dom matchers to `vitest.setup.ts`, switched the file's `jest.mock` → `vi.mock` (static hoist), included it. **12/12 green.** All 3 pre-existing `__tests__/` suites now run — frontend total **53 tests** (lib 4 + worker-compliance 21 + worker-offline 16 + audit-pack 12). Component-testing seam (RTL + jsdom) established.
- [x] **Pagination clamp on the other list endpoints** — clamped **audit-log** (was `limit||50`, allowed up to 1000) and **documents** (was `limit||20`, fully unbounded) to `page>=1` + `limit in [1,100]` via `Math.min(Math.max(...,1),100)`. (compliance + workers were already clamped; shifts has no user-controlled limit.) +5 route tests proving the audit-log clamp (oversized→100, default 50, in-range, negative→1, page floor). `test:ci` now 190.
- [x] **Unit tests for `lib/fetchWithRetry.js`** — 8 tests (success-first-try, non-retryable 404 no-retry, 5xx retry→success, network-error retry→success, 429 retryable, maxRetries:0 single attempt). **Pinned a contract quirk:** a persistent 5xx is *returned* (non-ok) after the last attempt, but a persistent thrown/network error is *re-thrown* (with `.attempts`) — callers must handle both. `test:ci` now 185.
- [x] **Worker document re-upload nudge** — the `REJECTED` re-upload CTA already existed; **added the missing expiring/expired case**: any document with an expiry that's `EXPIRED`/red/yellow (and not rejected) now shows a "This document has expired / is expiring soon — upload a renewed copy" nudge + Re-upload button. Extracted a shared `startReupload(documentTypeId)` (pre-fills the doc-type, smooth-scrolls to + focuses the upload form, toasts) used by both CTAs. Build clean.
- [x] **Structured logger (pino) — core + request correlation:** added `lib/logger.js` (pino; level from `LOG_LEVEL`/NODE_ENV; redacts authorization/cookie/password/otp/token). Wired into `server.js`: each request gets `req.log = logger.child({ requestId })`, and the global error handler now logs structured + correlated (`(req.log||logger).error({err,userId,agencyId}, …)`). +2 smoke tests. Establishes the structured-logging seam.
- [x] **Migrate remaining `console.*` to the logger** — **DONE.** All 24 `routes/*.js` (~138 calls) + 6 libs (`encryption`, `nodemailer`, `compliance-service`, `auth`, `compliance-assignment`, `email-templates` — ~24 calls) now log via pino (`(req.log||logger)` in handlers, base `logger`/child in libs, `(obj,msg)` form, secrets redacted, Sentry preserved). **Intentionally left as `console` (deliberate):** `server.js` boot messages, `worker-auth.js` module-scope JWT boot warning, `lib/fetchWithRetry.js` (low-level util whose unit test spies on console), `lib/analysis-failure.js` (last-resort fallback logs — console is the safe fallback if the logging/DB infra itself is failing), and everything under `src/tests/`.
- [x] **Finish `compliance-dashboard` module coverage** — +6 tests for the aggregation/formula functions: `calculateOverallCompliance` (empty → 0, rounded average), `groupByStatus` (partition + empty buckets), `verifyScoreFormula` (100 when none required, rounded %). The module (filters + sort + paginate + query-string + status + aggregation) is now fully unit-tested. Frontend `test:ci` 67 → 73.
- [x] **Replace the boilerplate root `README.md`** — the root README was still the default create-next-app template. Wrote a real project README: what ShiftWise is + the core promise, monorepo layout, tech-stack table, prerequisites, backend+frontend setup, env-var tables (verified against `.env.example`), scripts, testing/CI, and architecture notes (multi-tenancy, two audiences, compliance engine, document encryption, observability). Docs only.

## P8 — New feature: worker reliability scorecards (from IDEAS.md)
- [x] **Worker scorecards — backend slice:** `routes/worker-scorecards.js` (`GET /api/worker-scorecards`, agency-scoped) aggregates existing `ShiftAssignment` data per worker (total / confirmed / declined / pending + `confirmationRate` = confirmed of responded), sorted best-first. **No schema change** (reads existing data). +5 tests (`test:ci` 197). Foundation for the AI shift-matcher.
- [x] **Worker scorecards — frontend slice:** new `/dashboard/workers/scorecards` page — table of workers with assigned/confirmed/declined/pending counts + a colour-coded confirmation-rate badge (green ≥80 / amber ≥50 / red / grey "—" for no data), sorted best-first by the API. Uses `useApi`, `Skeleton` loading, `EmptyState` empty. Linked from the Workers header ("Reliability"). **Feature complete end-to-end.**
- [x] **Reliability at the point of assignment — backend slice:** `GET /api/shifts/:shiftId/assignable-workers` now enriches each compliant candidate with `confirmationRate` (one additive `groupBy`; null when no responded history), so coordinators can weigh reliability while picking. +1 test (`test:ci` 198).
- [x] **Reliability at the point of assignment — frontend slice:** `AssignModal` now shows a reliability badge next to compliance for each candidate — colour-coded `{rate}% conf` (green ≥80 / amber ≥50 / red), or a muted **"New"** badge when there's no confirmation history yet. **Feature complete end-to-end** (coordinators see reliability inline while assigning).
- [x] **Rule-based "suggested workers" hint** — `assignable-workers` now sets `suggested` per candidate (compliant — all are — AND `confirmationRate ≥ 80`; new/no-history workers aren't flagged). `AssignModal` shows a "⭐ Suggested" badge on those. +2 test assertions. **Complete end-to-end.** Chose a per-candidate flag/badge over a reorder to avoid fighting the endpoint's firstName pagination. *(Next, bigger: a true global "best candidates" ranking would need the endpoint to fetch-all → rank → paginate; that + weighting distance/skills is the AI shift-matcher, likely its own milestone.)*

## P9 — New feature: shift coverage / staffing gaps (no migration)
- [x] **Shift coverage — backend slice:** `routes/shift-coverage.js` (`GET /api/shift-coverage`, agency-scoped) returns upcoming shifts (shiftDate ≥ today) with required vs assigned vs confirmed counts, `shortfall`, and a `status` (filled / understaffed / unfilled), sorted soonest-first, plus a `{ totalUpcoming, needingAttention }` summary. Pure aggregation of existing Shift + ShiftAssignment data — **no schema change**. +4 tests (`test:ci` 202).
- [x] **Shift coverage — frontend slice:** new `/dashboard/shifts/coverage` page — upcoming shifts table (date/facility/role, confirmed÷required, shortfall, status badge: filled/understaffed/unfilled), a "X of Y need attention" summary, and a "show only needing attention" filter. `useApi`, `Skeleton`, `EmptyState` (distinct empty vs all-covered messages). Linked from the Shifts header ("Coverage"). **Feature complete end-to-end.**
- [x] **Shift coverage — surface on the main dashboard:** the dashboard now fetches `/api/shift-coverage` alongside its stats and shows an amber **actionable alert** ("N upcoming shift(s) need workers → Review coverage", links to the coverage page) — only when `needingAttention > 0`, so the home screen stays clean when fully staffed. Coordinators see gaps the moment they log in.

## P10 — Reliability on the worker profile
- [x] **Per-worker scorecard — backend slice:** `GET /api/worker-scorecards/:workerId` (agency-scoped, 404 if not in agency) returns a single worker's reliability (total/confirmed/declined/pending + `confirmationRate`) via `groupBy(['workerConfirmation'])`. +3 tests (`test:ci` 205).
- [x] **Per-worker scorecard — profile surfacing:** the worker detail page (`/dashboard/workers/[id]`) now fetches `/api/worker-scorecards/:id` (added to its parallel load) and shows a **"Reliability" panel** — big colour-coded confirmation-rate % (green ≥80 / amber ≥50 / red, or "No history yet") + assigned/confirmed/declined/pending breakdown. **Feature complete end-to-end.**
- [x] **Discoverability: add new views to ⌘K command palette** — added "Worker Reliability" (`/dashboard/workers/scorecards`) and "Shift Coverage" (`/dashboard/shifts/coverage`) to the command-palette nav with icons + search keywords, so the new pages are quick-navigable (they were previously only reachable via in-page header links).
- [x] **DRY the reliability rate→colour logic** — extracted `frontend/lib/reliability.ts` (`reliabilityRateStyle(rate)` → `{ label, badgeClass, textClass }`, single source of truth for the green≥80/amber≥50/red/grey thresholds) with +4 unit tests, and adopted it in the scorecards page + worker-profile panel (removing two divergent copies). `AssignModal` keeps its own generic-Tailwind badge to match its compliance badge.

## P11 — Core promise: expiring-documents worklist
- [x] **Expiring documents worklist — backend slice:** `routes/expiring-documents.js` (`GET /api/expiring-documents?days=30`, agency-scoped, days clamped 1..365) returns a **flat, urgency-sorted** list of active workers' compliance docs that are overdue or expiring within the window — each with `daysUntilExpiry` (negative = overdue) + `overdue` flag + a `{ total, overdue, windowDays }` summary. **Includes already-expired** docs (unlike the report), since overdue = an active compliance breach. Reads existing data, no migration. +4 tests (`test:ci` 209).
- [x] **Expiring documents worklist — frontend slice:** new `/dashboard/documents/expiring` view — overdue-first table (worker → profile link, document type, expiry date + a coloured "Xd left / Expired Xd ago / Expires today" label, status), a **days-window selector** (7/30/60/90), an "N overdue, M within Xd" summary, and overdue rows tinted red. `useApi` + `Skeleton` + `EmptyState`. Re-pointed the dashboard **"EXPIRING SOON"** card to it (was → `/documents`). **Core-promise feature complete end-to-end.**
- [x] **Expiring worklist — discoverability:** added an "Expiring & overdue" link to the Documents page header and an "Expiring Documents" entry to the ⌘K command palette (Clock icon + keywords). The core worklist is now reachable from the dashboard card, the documents page, and quick-nav.

## P12 — AI shift-matcher (rule-based MVP)
- [x] **Shift-matcher — backend slice:** `GET /api/shifts/:shiftId/suggested-workers?limit=5` (OWNER/ADMIN) returns the top compliant candidates ranked by the **documented default** (confirmation-rate desc, no-history last, then compliance score, then name). Uses the batched `validateComplianceForWorkers` (~constant queries) + one `groupBy` for reliability; candidate scan capped at 200 with `meta.scanCapped`. +2 tests (`test:ci` 211). **Ranking weights are a sensible default — tunable when the founder specifies.**
- [x] **Shift-matcher — frontend slice:** `AssignModal` now shows a "⭐ Top picks for this shift" strip (from `/suggested-workers`) — ranked candidate chips (name + reliability %, compliance/reliability in the tooltip), each **one-click selectable** (toggles into the assignment, filled when selected). Fetched on open; hidden + non-blocking if there are none/it fails. **Shift-matcher MVP complete end-to-end** (weights still tunable on founder preference).
- [x] **Shift-matcher — extract ranking to a pure, tested function:** moved the inline sort into `backend/src/lib/rank-suggested-workers.js` (`rankSuggestedWorkers`, non-mutating) and refactored the route to call it. +6 unit tests covering rate-desc, null-last, compliance tie-break, name tie-break, two-nulls stability, and no-mutation (`test:ci` 217). **⭐ When the founder gives ranking weights, this one function is the only place to change** (and where distance/skills/rotation get layered in).

## P13 — Frontend page-level test coverage
- [x] **Establish page/component tests for app routes** — widened `vitest.config.ts` `include` to `app/**/*.test.{ts,tsx}` and added a first page test: `app/dashboard/shifts/coverage/page.test.tsx` (mocks `useApi`, asserts a coverage row renders with facility/status/counts, and the empty state shows when there are no upcoming shifts). Confirms client pages render under jsdom+RTL. Frontend `test:ci` 77 → 79.
- [x] **Extend page tests to the other new views** — added `workers/scorecards/page.test.tsx` (row + confirmation-rate, empty state), `documents/expiring/page.test.tsx` (overdue row + urgency label, empty state), and `dashboard/page.test.tsx` (coverage alert shows when shifts need attention / hidden when none — mocks Clerk `useAuth` + `useApi` routed by URL). All four new feature surfaces now have page-level tests. Frontend `test:ci` 79 → 85.

## P14 — Accessibility
- [x] **A11y pass on the new feature surfaces** — added `scope="col"` to all 16 table headers across the coverage, scorecards, and expiring-documents views (correct header association for screen readers), and toggle semantics for assistive tech: `aria-pressed` + descriptive `aria-label` on the expiring-worklist window buttons (7/30/60/90d) and on the shift-matcher "⭐ Top picks" chips. Behaviour-preserving (85 tests still green).
- [x] **A11y sweep of the older core pages (table headers)** — added `scope="col"` to every remaining dashboard table header across audit-log, audit-packs, compliance, compliance-settings, documents, reports, and workers. Confirmed zero `<th>` without `scope` remain anywhere in `app/dashboard` (10 files covered). Behaviour-preserving (85 tests green).
- [x] **A11y — icon-only buttons + contrast audit** — (1) verified via app-wide regex that **no unlabeled icon-only buttons remain** (close/download/replace/etc. already carry `aria-label`), so nothing to add there. (2) Computed WCAG contrast for every status/badge colour pair; all passed except the grey "no-history / not-uploaded / inactive" badge (`#5B6E8C` on `#EBEEF5` = **4.46, just under AA 4.5**). Fixed by darkening the text to **`#52627E` (5.31:1)** everywhere it occurs — the shared `status-badge` component, `lib/reliability.ts`, and the worker-profile badges — and updated the reliability unit test. All status badges now clear AA.

## P15 — Documentation
- [x] **API reference for the session's new endpoints** — added `docs/API.md` documenting the six insight/matching endpoints built this session (shift-coverage, worker-scorecards + `:workerId`, expiring-documents, suggested-workers): method, auth, query params, and exact response shapes. Verified every documented shape against the route code (status enums, summary/meta keys) to avoid drift.

## P16 — Bug fixes (self-review)
- [x] **Deactivated workers were still assignable to shifts (candidate lists)** — `deactivate` sets `status='INACTIVE'` (never touches `isActive`), but all three shift-assignment routes (`assignable-workers`, `assign-bulk`, `suggested-workers`) filtered only on `isActive: true` — so a deactivated worker still appeared in the assignable list, bulk-assign candidates, **and** the shift-matcher "top picks". Added `status: 'ACTIVE'` to all three filters (matches the canonical filter used by reports/dashboard/deactivate) and a regression assertion on the suggested-workers where-clause. Found via a self-review bug-hunt of this session's code; fixes a real correctness/safety hole (could assign a worker the agency had deactivated).
- [x] **Deactivated workers were still assignable via the write path (defense-in-depth)** — the candidate-list fix didn't cover the actual assign endpoints: `POST /assign` validated only agency membership (not status), and `POST /assign-bulk` used `validateComplianceForWorkers`, whose worker fetch had no status filter — so a **direct API call** (or stale UI) could still assign a deactivated worker. Added an explicit `status !== 'ACTIVE'` → 400 ("Cannot assign a deactivated worker") in `POST /assign`, and `status: 'ACTIVE'` to the validator's worker fetch (deactivated → `notFound` → skipped in bulk). +1 regression test; also fixed the happy-path test's worker mock (now needs `status: 'ACTIVE'`). `test:ci` 218.
- [x] **Off-by-one: documents expiring *today* were flagged overdue** — `expiring-documents` computed `daysUntilExpiry = floor((expiryDate_midnightUTC − now_currentTime)/day)`, so a doc expiring today came out as `−1` → `overdue: true` / "Expired 1d ago" (and inflated the `overdue` count) even though it's still valid today. Fixed to a calendar-day diff (both normalized to UTC midnight, `Math.round`): today → `0` / not overdue, yesterday → `−1` / overdue. +1 regression test (`test:ci` 219).
- [x] **IDOR sweep of by-ID/download endpoints** — audited the other object-by-ID paths after the audit-pack IDOR: `documents /:id/download` (decrypt+stream), `documents /:id/status|/analyse|/verify`, and `shift-assignments /assignments/:assignmentId` GET+DELETE are all correctly agency-scoped (`findFirst {id, agencyId}` → 404, no existence leak). **No further IDOR** — audit-pack was the lone outlier. (Note: `documents.js` can't be unit-tested via supertest yet — it imports `ocrService.ts` and the backend jest has no TS transform; that's why `file-download.test.js` is tautological and `security-pipeline` is excluded. Real documents-route tests need the jest TS-transform, a separate infra task.)
- [x] **Empty agency falsely reported "CQC ready" (green)** — `GET /readiness` computed `readyForCQC = expiredCount === 0 && compliantCount === workers.length`, which is vacuously **true for 0 workers** → a brand-new empty agency showed 🟢 "Ready for CQC" (false confidence). Added a `workers.length > 0` guard (empty → yellow). +4-test `compliance-checklist.js` suite (empty-not-green, green/red/yellow, div-by-zero-safe %). `test:ci` 263. **⚠ Also flagged (not changed): `/readiness` `hasExpired` counts ANY expired doc (incl. optional/rejected) toward "red" — arguably should be required+approved only; a compliance-semantics call for the founder.**
- [x] **🔴 SECURITY (IDOR): cross-agency audit-pack download** — `GET /api/agency/audit-pack/download/:packId` called `downloadAuditPack(packId)` with **no agency check**; the service just streamed `uploads/audit-packs/{packId}.zip` off disk. The route only requires OWNER/ADMIN of *some* agency, so a coordinator of agency A could download agency B's audit pack (B's workers' DBS/passport/RTW documents + PII) by knowing/guessing the packId — a cross-tenant data leak in a healthcare compliance product. **Fix:** every packId now embeds the owning `agencyId` (single-worker packId was `audit-pack-<workerId>-<ts>` → now `audit-pack-<agencyId>-<workerId>-<ts>`; bulk already had it); `downloadAuditPack(packId, agencyId)` rejects packs whose packId doesn't start with the caller's `audit-pack-<agencyId>-`/`bulk-export-<agencyId>-` prefix (trailing '-' prevents id-prefix collisions), same 404 message so existence isn't leaked. Guard extracted to a dep-free `lib/audit-pack-ownership.js` + 5 unit tests (incl. the prefix-collision case). Route passes `req.agencyId`. `test:ci` 259.
- [x] **Bug-class sweep: unsafe `.trim()` on possibly-null/non-string input** — audited every `.trim()` in `routes/services/lib`. The `X !== undefined` + `X.trim()` null-crash class is now fully remediated (only the already-fixed `workers`/`agencies` PATCHes match). All other `.trim()`s are guarded (truthy checks, `String()` coercion, template literals, `x ? x.trim() : null`) **except** `worker-auth` verify-code, which checked `!email` but not `typeof email === 'string'` (unlike signin) → a non-string `email` (e.g. `123`) hit `(123).toLowerCase()` → 500 instead of 400. Fixed the guard + added a regression test. `test:ci` 254.
- [x] **Agency settings `PATCH /update` crashed on null fields** — same class as the `workers` PATCH bug: `address/city/postcode/phone/agencyType` used `!== undefined` + `.trim()`, so clearing an optional field with `PATCH { address: null }` threw `null.trim()` → 500. Guarded all five with `x === null ? null : x.trim()`. Added a 6-test `agencies.js` suite (previously untested): null-field regression, string trimming, thresholds validation (non-array / out-of-range / non-integer → 400) + valid save (agency-scoped map). `test:ci` 253.
- [x] **Dashboard "EXPIRING SOON" count dropped documents expiring today** — same date class as the report bug: `dashboard/stats` counted `expiryDate: { gte: now }` (current time), excluding docs expiring today (UTC-midnight < now) → the headline dashboard count under-reported the most urgent renewals. Fixed to `gte: startOfToday` (UTC midnight). +3-test `dashboard.js` suite (previously untested): stats shape + agency-scoping, start-of-day window, graceful 500. `test:ci` 266. *(Assessed enabling a jest TS-transform to test `documents.js` — the backend has no babel/ts deps installed and runs via plain node, so it's a risky infra change; left as a founder-gated task.)*
- [x] **Expiring *report* dropped documents expiring today** — `GET /api/reports/expiring` filtered `expiryDate: { gte: new Date() }` (current time); since `expiryDate` is stored at UTC midnight, a document expiring **today** was `< now` → silently missing from the report (the most urgent renewals). Fixed to `gte: startOfToday` (UTC midnight) — includes today + future, still excludes already-lapsed. +3 tests for the previously-untested `reports.js` (`test:ci` 247). Same class as the worklist off-by-one but on a separate surface.
- [x] **`checkWorkerCompliance` logged via out-of-scope `req`** — the module-level helper `checkWorkerCompliance(workerId, agencyId)` (used by `POST /assign`) had `(req.log || logger)` in its catch, but there's no `req` in that scope — so on a compliance-check DB error it threw `ReferenceError: req is not defined`, **masking the real error** in logs (a pino-migration slip). Changed to the base `logger`. Verified via grep that this was the only helper (non-`(req,res)` scope) referencing `req.log`. +1 test exercising the compliance-error path → graceful 500 (`test:ci` 220).

## P17 — Test coverage for untested authz endpoints
- [x] **Bug + tests for `workers.js` (was untested)** — **Bug:** `PATCH /api/workers/:id` did `phone.trim()`/`notes.trim()` guarded only by `!== undefined`, but create allows `phone/notes = null`, so `PATCH { phone: null }` (clearing a field — the comment literally says "Allow emptying") threw `TypeError` → 500. Fixed to `x === null ? null : x.trim()`. Added an 8-test suite (create validation/duplicate-409/201, GET/PATCH/DELETE ownership 404s, delete success, and the null-field regression). `test:ci` 230 → 238.
- [ ] **⚠ Finding (needs founder confirm): `reactivate` has no role check.** `PATCH /:id/deactivate` requires `OWNER/ADMIN`, but `PATCH /:id/reactivate` is `requireAgency` only — so a lower-privilege coordinator can undo a deactivation (make a worker assignable again) that an admin made. Very likely an oversight (inverse ops should match), but tightening authz could break an intended workflow, so leaving the decision to the founder. Fix = add `requireRole(['OWNER','ADMIN'])` to reactivate.
- [x] **Test suite for `compliance.js` state-changers (was untested)** — reviewed approve/reject-document + deactivate-worker: all correctly agency-scoped (`findFirst {id, agencyId}` → 404) + role-gated (`OWNER/ADMIN`) + audit-logged — **no bug** (reject-reason is optional by design, not a defect). Added a 6-test suite locking the 404-for-out-of-agency-target authz + approve/reject/deactivate happy paths (correct status + audit log). `test:ci` 238 → 244.
- [x] **Test suite for `worker-availability` (was untested)** — reviewed the coordinator availability CRUD (`GET/POST/DELETE /api/workers/:workerId/availability`): authorization is correct (agency-scoped, verifies worker-in-agency → 404), no bug found — but it had **zero tests**. Added a 10-test suite covering the 404-for-out-of-agency-worker authz on all three verbs, input validation (missing fields / invalid status / invalid date), status upper-casing, the date-range filter, upsert, delete, and the P2025 not-found path. `test:ci` 220 → 230. (Also reviewed `worker-assignments` in the same pass — clean + already well-tested, incl. the cross-worker 403.)
